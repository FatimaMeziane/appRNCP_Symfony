
<nav
	class="navbar navbar-expand-lg navbar-dark bg-warning ">
	{# <nav class="navbar navbar-expand-lg bg-light" data-bs-theme="dark"> #}
	<div class="container-fluid divFirstNav">
		<a class="navbar-brand" href="{{ path('home.index') }}">
			<img src="{{ asset('images/logoClubRecette2.png') }}" class="LogoClubRecettes" alt="Logo" class="d-inline-block align-text-top">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse bg-warning " id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('home.index') }}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
					<li class="nav-item">
					<a class="nav-link active" href="{{ path('home.index') }}">À propos
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{# je test si je suis connecte ou pas #}
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link active" href="{{ path('recipe.index') }}">Mes Recettes
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active" href="{{ path('ingredient.index') }}">Mes Ingredients
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					{% else %}
						<li class="nav-item">
						<a class="nav-link active" href="{{ path('recipe.index') }}">Recettes
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					{# <li class="nav-item">
						<a class="nav-link active" href="{{ path('ingredient.index') }}">Ingredients
							<span class="visually-hidden">(current)</span>
						</a>
					</li> #}
				{% endif %}

			</ul>

			<div class="d-flex bg-warning">
				<ul class="navbar-nav  me-auto">
					{% if app.user %}
						<li class="nav-item dropdown bg-warning me-5 pe-5 " >
						{# style="margin-right: 70px;" #}
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
							<div class="dropdown-menu bg-warning border-0 px-2">
								<a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id})}}">Modifier mes information</a>
								<a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id})}}">Modifier mon mot de passe</a>
								<div class="dropdown-divider"></div>
								{% if 'ROLE_ADMIN' in app.user.roles %}
									<a class="dropdown-item bg-warning" href="{{ path('admin') }}">Administration</a>
									<div class="dropdown-divider"></div>
								{% endif %}
								<a class="dropdown-item" href="{{ path('security.logout')}}">Déconnexion</a>
							</div>
						</li>
					{% else %}
						<li class="nav-item bg-warning">
							<a class="nav-link active" href="{{ path('security.login') }}">Se connecter
								<span class="visually-hidden">(current)</span>
							</a>
						</li>
						<li class="nav-item bg-warning">
							<a class="nav-link active" href="{{ path('security.registration') }}">S'inscrire
								<span class="visually-hidden">(current)</span>
							</a>
						</li>
					{% endif %}

				</ul>
			</div>
		</div>
	</div>
</nav>

{# <nav>
 <ul class="menu-items">
    <li><a href="#">Entrée</a></li>
    <li><a href="#">Plat</a></li>
    <li><a href="#">Boisson</a></li>
    <li><a href="#">Accompagnement</a></li>
    <li><a href="#">Goûter</a></li>
    <li><a href="#">Dessert</a></li>
  </ul>
</nav> #}


{# <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom border-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('images/logoClubRecette2.png') }}" class="d-block" alt="Logo" />
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item">
                    <a class="nav-link active mx-5" aria-current="page" href="#!">Sneakers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active mx-5" aria-current="page" href="#!">Réalisations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active mx-5" aria-current="page" href="#!">Mon compte</a>
                </li>
            </ul>
            <form class="d-flex">
                <button class="btn btn-outline-success" type="submit" aria-label="Panier">
                    <div class="row">
                        <div class="col-8">
                            <img src="{{ asset('assets/uploads/image/panier1.svg') }}" class="d-block" alt="Image du panier" />
                        </div>
                        <div class="col-4">
                            <span class="badge bg-dark text-white ms-1 rounded-pill" aria-label="Nombre d'articles dans le panier">0</span>
                        </div>
                    </div>
                </button>
            </form>
        </div>
    </div>
</nav> #}


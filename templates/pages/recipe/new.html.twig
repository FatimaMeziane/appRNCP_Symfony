{% extends "base.html.twig" %}
{% block title %}
	SymRecipe - Nouvelle recette
{% endblock %}
{# pour dire a twig qu'on utilise le theme boodstrap #}
{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}
	<div class="container">

		<h1 class="mt-4">Création d'une recette</h1>

		<div class="form-check mt-4">
			{{ form_label(form.isFavorite) }}
			{{ form_widget(form.isFavorite) }}
			<div class="my-custom-class-for-errors">
				{{ form_errors(form.isFavorite) }}
			</div>
		</div>
		{{ form_start(form) }}
		{{ form_widget(form)}}
		{#  si on veut  personalisé notre formulaire 
															        <div class="form-group">
																		{{ form_label(form.name) }}
																		{{ form_widget(form.name) }}
																		<div class="my-custom-class-for-errors">
																			{{ form_errors(form.name) }}
																		</div> #}

		{# </div>
																	<div class="form-group">
																		{{ form_label(form.price) }}
																		{{ form_widget(form.price) }}
																		<div class="my-custom-class-for-errors">
																			{{ form_errors(form.price) }}
																		</div>
															
																	</div> #}
			<button type="submit" class="btn btn-success mt-4"> Créer une recette</button>
		{# on affiche le button ici si on lavais rajouter dans la classType
									<div class="form-group">
						             {# {{ form_row(form.submit)}} #}
		{# </div> #}

		{{form_end(form)}}
	</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}SymRecipe - Modification du mot de passe
{% endblock %}
{% block body %}
		<h1 class="mt-4 text-center">Réinitialiser mon mot de passe</h1>
	<div class="container w-75">

		{% for message in app.flashes('warning') %}
			<div class="alert alert-warning mt-4">
				{{ message }}
			</div>
		{% endfor %}

		{{ form(form) }}
		</div>
	{% endblock %}

	{# {% block body %}
		<div class="container">
			<h1 class="mt-4">
				Formulaire de modification du mot de passe
			</h1>
			{% for message in app.flashes('warning') %}
				<div class="alert alert-warning mt-4">
					{{ message }}
				</div>
			{% endfor %}
			{{ form_start(form) }}
			{{ form_widget(form)}}
			{{ form_end(form)}}
	
	
		</div>
	
	{% endblock %} #}
